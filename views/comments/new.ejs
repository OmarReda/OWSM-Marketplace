<%- include('../partials/header')-%>
<section id="new-item-form">
    <div class="container">
        <div class="row">
            <div class="form-container">
                <h1>Add New Question or Review to <%= campground.name %></h1>
                <form action="/campgrounds/<%= campground._id %>/comments" method="POST">
                    <input class="form-control" type="text" name="comment[text]" placeholder="Comment" required>
                    <input class="form-control" type="text" name="comment[author]" placeholder="Author" required>
                    <div class="button-group">
                        <button class="btn bt-lg btn-primary">Submit</button>
                        <a class="btn bt-lg btn-danger" href="/campgrounds/<%= campground._id %>">Go Back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
<%- include('../partials/footer'); -%>